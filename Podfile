# Require platform and version
platform :ios, '9.0'

# Indicate that you're using dynamic frameworks
use_frameworks!

# Start the target block for 'MallApp'
target 'MallApp' do
  # List of dependencies for 'MallApp'
  pod 'Kingfisher'
  pod 'SnapKit'
  pod 'R.swift'
end
